# Jarvis 4.0 – Python Dependencies
# AWP-039: Refactoring-Run | Gepinnte Versionen für Reproduzierbarkeit
# Install: pip install -r requirements.txt
# Audit:   pip install pip-audit && pip-audit -r requirements.txt

# ─── Core API ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9          # für FastAPI file uploads

# ─── RAG / Vector DB ───────────────────────────────────────────────────────
qdrant-client==1.9.1
chroma-hnswlib==0.7.5            # 0.7.3 hat kein pre-built Windows wheel → 0.7.5 verwenden
chromadb==0.5.0                  # install: pip install chromadb==0.5.0 --no-deps (dann deps manuell)
numpy==1.26.4                    # chromadb 0.5.0 inkompatibel mit numpy>=2.0 (np.float_ entfernt)
pymupdf==1.24.3                  # AWP-103: PDF-Parser (fitz), Python 3.12 kompatibel
sentence-transformers==3.0.1
torch==2.3.0                     # CPU-only: torch==2.3.0+cpu (install via extra index)

# ─── HTTP / Async ──────────────────────────────────────────────────────────
aiohttp==3.9.5
httpx==0.27.0                    # für interne Service-Kommunikation

# ─── System Monitoring ─────────────────────────────────────────────────────
psutil==5.9.8                    # Thread-Monitor (AWP-037)

# ─── Security / Linting ────────────────────────────────────────────────────
ruff==0.4.4                      # Linting (AWP-033 Tester)
# bandit==1.7.8                  # Optional: zusätzliche SAST-Analyse

# ─── Testing ───────────────────────────────────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.6

# ─── Logging ───────────────────────────────────────────────────────────────
# Fluent Bit läuft als Container – kein Python-Paket nötig
# python-json-logger==2.0.7      # Optional: strukturiertes JSON-Logging

# ─── YAML ──────────────────────────────────────────────────────────────────
# Kein PyYAML nötig – librarian.py parst Frontmatter ohne externe Deps
